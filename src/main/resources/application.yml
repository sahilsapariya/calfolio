spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/calculator_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  application:
    name: calculator-app

server:
  port: 8080
